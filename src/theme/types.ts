export interface Theme {
  colors: {
    primary: string;
    secondary: string;
    background: string;
    text: string;
    light: string;
    green: string;
  };
  typography: {
    text_xs: object;
    text_sm: object;
    text_md: object;
    text_lg: object;
  };
}
